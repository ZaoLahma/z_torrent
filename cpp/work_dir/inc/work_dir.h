#pragma once

#include "config.h"

namespace ztorrent
{
    class WorkDir
    {
        public:
        WorkDir() = delete;
        WorkDir(const Config& config);

        protected:

        private:
    };
}